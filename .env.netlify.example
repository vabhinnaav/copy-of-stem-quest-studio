# Required by the stateless question token service. Set a long random value in Netlify.
JWT_SECRET=replace-with-a-long-random-secret

# Optional. Required only when you want a project-default Gemini fallback.
# Learners can instead supply their own provider key for the browser session.
GEMINI_API_KEY=
